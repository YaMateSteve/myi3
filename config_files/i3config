
set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Cantarell Regular 12

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

######################
## PROGRAM BINDINGS ##
######################
set $term     exec urxvt -name terminal
set $altterm  exec urxvt
set $browser  exec firefox
set $browser_class Firefox 

## DROPDOWN WINDOWS ##

# Dropdown terminal - $mod+u #
for_window [instance="dropdown"] floating enable
for_window [instance="dropdown"] resize set 625 460
for_window [instance="dropdown"] move scratchpad
for_window [instance="dropdown"] border pixel 3
exec --no-startup-id $altterm -name dropdown

# Math teminal (python shell) - $mod+i #
for_window [instance="math"] floating enable
for_window [instance="math"] resize set 1000 500
for_window [instance="math"] move scratchpad
for_window [instance="math"] border pixel 3
exec --no-startup-id $altterm -name math -fn xft:monaco:pixelsize=24 -e python -q



##################
## KEY BINDINGS ##
##################

# Open terminal
bindsym $mod+Return $term

## switch windows back and forth
bindsym $mod+Tab workspace back_and_forth

# kill focused window
bindsym $mod+Shift+q kill

# Atom
bindsym $mod+a exec atom
for_window [class="Atom"] border pixel 0

# dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -l 10

# Math terminal (python shell)
bindsym $mod+i [instance="math"] scratchpad show; [instance="math"] move position center

# Libre office
bindsym $mod+o exec libreoffice --nologo
for_window [class="libreoffice"] border pixel 0

# Python idle editorbindsym $mod+Shift+h move left 30
bindsym $mod+p exec idle -e
for_window [class="Toplevel"] border pixel 0
#for_window [class="Toplevel"] floating enable move right 361px, move up 54px
for_window [class="Toplevel"] resize set 600 700

# Turtle window
for_window [class="Tk"] floating enable

# Ranger
bindsym $mod+r $term -e ranger

# Dropdown terminal
bindsym $mod+u [instance="dropdown"] scratchpad show; [instance="dropdown"] move position center

# Change gaps
bindsym $mod+z gaps outer current plus 5; gaps inner current plus 5
bindsym $mod+x gaps outer current minus 5; gaps inner current minus 5

# Move window
bindsym $mod+Shift+Left move left 30
bindsym $mod+Shift+Down move down 30
bindsym $mod+Shift+Up move up 30
bindsym $mod+Shift+Right move right 30

# Resize window
bindsym $mod+Shift+h resize shrink width
bindsym $mod+Shift+j resize shrink height
bindsym $mod+Shift+k resize grow height
bindsym $mod+Shift+l resize grow width
bindsym $mod+Shift+z resize grow height; resize grow width
bindsym $mod+Shift+x resize shrink height; resize shrink width

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right


# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+b focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1: "
set $ws2 "2: "
set $ws3 "3: "
set $ws4 "4"
set $ws5 "5: "
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10: Config"

# switch to workspace
bindsym $mod+1 workspace $ws1; exec ~/myi3/config_files/scripts/browser.sh
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3; exec ~/myi3/config_files/scripts/atom.sh
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# Set colours

set $bg-color            #2f343f
set $inactive-bg-color   #2f343f
set $text-color          #f3f4f5
set $inactive-text-color #676e7d
set $urgent-bg-color     #e53935

#window colours
#                       Border             Background         Text                 Indicator
client.focused          $bg-color          $bg-color          $text-color          #00ff00
client.unfocused        $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
client.focused_inactive $inactive-bg-color $inactive-bg-color $inactive-text-color #00ff00
client.urgent           $urgent-bg-color   $urgent-bg-color   $text-color          #00ff00

###############
### BORDERS ###
###############

gaps outer 10
gaps inner 10
for_window [class="^.*"] hide_edge_borders both
for_window [instance="terminal"] border pixel 0
for_window [class="$browser_class"] border pixel 0; hide_edge_borders both




# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command i3status
	position top
	font pango: Cantarell, Font Awesome 5 Free 12

        colors{
               background $bg-color
               separator  #757575
               #                  Border             Background         Text
               focused_workspace  $bg-color          $bg-color          $text-color
               inactive_workspace $inactive-bg-color $inactive-bg-color $inactive-text-color
               urgent_workspace   $urgent-bg-color   $urgent-bg-color   $text-color
        }
}

###########################
### WALLPAPER / EFFECTS ###
###########################

exec --no-startup-id feh --bg-max ~/Pictures/image4.jpg
exec compton -f

#######################
## START-UP PROGRAMS ##
#######################

exec --no-startup-id nm-applet
#exec --no-startup-id i3-msg 'workspace $ws1; $browser'
#exec --no-startup-id i3-msg 'workspace $ws2; $term'
exec --no-startup-id i3-msg 'workspace $ws10; exec gedit ~/.config/i3/config + ~/.Xresources + ~/.bashrc + ~/myi3/keybindings.txt'

### Assign workspaces ###

assign [class="$browser_class"] $ws1
assign [class="Atom"] $ws3 
assign [class="libreoffice"] $ws5


### Audio ###

# Pulse Audio mechanical buttons config
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Other stuff
workspace_auto_back_and_forth yes

